'use strict';
module.exports = {
  up: (queryInterface, Sequelize) => {
    return Promise.resolve()  // this is just to fake this migration untill i figure out how to delete it
  },
  down: (queryInterface, Sequelize) => {
    return Promise.resolve()
  }
}